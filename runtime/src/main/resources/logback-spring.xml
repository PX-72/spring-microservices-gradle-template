<?xml version="1.0" encoding="UTF-8"?>
<configuration>

  <!-- Spring adds useful properties like spring.application.name -->
  <springProperty scope="context" name="serviceName" source="spring.application.name" defaultValue="service"/>

  <property name="CONSOLE_PATTERN"
            value="%d{yyyy-MM-dd'T'HH:mm:ss.SSSXXX} %-5level [%thread] %logger{36} - %msg traceId=%X{traceId:-} spanId=%X{spanId:-}%n"/>

  <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
    <encoder>
      <pattern>${CONSOLE_PATTERN}</pattern>
    </encoder>
  </appender>

  <springProfile name="json">
    <appender name="JSON" class="ch.qos.logback.core.ConsoleAppender">
      <encoder class="net.logstash.logback.encoder.LoggingEventCompositeJsonEncoder">
        <providers>
          <timestamp/>
          <logLevel/>
          <threadName/>
          <loggerName/>
          <message/>
          <mdc/>
          <stackTrace/>
          <pattern>
            <pattern>
              {
                "service":"${serviceName}",
                "traceId":"%X{traceId:-}",
                "spanId":"%X{spanId:-}"
              }
            </pattern>
          </pattern>
        </providers>
      </encoder>
    </appender>

    <root level="INFO">
      <appender-ref ref="JSON"/>
    </root>
  </springProfile>

  <springProfile name="!json">
    <root level="INFO">
      <appender-ref ref="CONSOLE"/>
    </root>
  </springProfile>

</configuration>
